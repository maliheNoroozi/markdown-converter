import {
  HtmlConverter,
  PdfConverter,
  PowerpointConverter,
  WordConverter,
} from "@/components";

export const Converter = {
  html: HtmlConverter,
  pdf: PdfConverter,
  powerpoint: PowerpointConverter,
  word: WordConverter,
};
